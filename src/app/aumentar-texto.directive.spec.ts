import { AumentarTextoDirective } from './aumentar-texto.directive';

describe('AumentarTextoDirective', () => {
  it('should create an instance', () => {
    const directive = new AumentarTextoDirective();
    expect(directive).toBeTruthy();
  });
});
