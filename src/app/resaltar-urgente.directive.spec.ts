import { ResaltarUrgenteDirective } from './resaltar-urgente.directive';

describe('ResaltarUrgenteDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarUrgenteDirective();
    expect(directive).toBeTruthy();
  });
});
